name: mapnavigatorapp
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  go_router: ^14.5.0
  geolocator: ^13.0.1
  sensors_plus: ^6.0.1
  google_maps_flutter: ^2.9.0
  permission_handler: ^11.3.1
  google_fonts: ^6.3.2
  flutter_dotenv: ^6.0.0
  get_it: ^8.2.0
  flutter_compass: ^0.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
